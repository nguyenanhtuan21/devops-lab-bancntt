version: '3.4'

services:
  firstapp:
    image: firstapp
    build:
      context: lab01/first-app
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 5001:5001
